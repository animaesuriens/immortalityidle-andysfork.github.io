---
phase: 01-duration-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/game-state/activity.ts
  - src/app/game-state/activity.service.ts
  - src/app/game-state/hell.service.ts
autonomous: true

must_haves:
  truths:
    - "Activity interface requires duration field"
    - "All 69 activity definitions include duration: 1"
    - "TypeScript compiles with no errors"
    - "Game runs exactly as before"
  artifacts:
    - path: "src/app/game-state/activity.ts"
      provides: "Activity interface with duration field"
      contains: "duration: number"
    - path: "src/app/game-state/activity.service.ts"
      provides: "46 activity definitions with duration"
      contains: "duration: 1"
    - path: "src/app/game-state/hell.service.ts"
      provides: "23 activity definitions with duration"
      contains: "duration: 1"
  key_links:
    - from: "src/app/game-state/activity.service.ts"
      to: "src/app/game-state/activity.ts"
      via: "implements Activity interface"
      pattern: "Activity"
    - from: "src/app/game-state/hell.service.ts"
      to: "src/app/game-state/activity.ts"
      via: "implements Activity interface"
      pattern: "Activity"
---

<objective>
Add duration field to Activity interface and all 69 activity definitions.

Purpose: Lay foundation for multi-day activities in later phases. All activities get duration: 1 (one day).
Output: Updated interface and activity definitions. Game runs exactly as before with no behavior change.
</objective>

<execution_context>
@C:\Users\marce\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\marce\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-duration-foundation/01-CONTEXT.md
@.planning/phases/01-duration-foundation/01-RESEARCH.md
@src/app/game-state/activity.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add duration field to Activity interface</name>
  <files>src/app/game-state/activity.ts</files>
  <action>
Add `duration: number;` to the Activity interface immediately after the `activityType: ActivityType;` line (line 82).

The field should be placed before `description` to group with behavioral fields per CONTEXT.md decision.

No JSDoc comment needed (existing interface fields don't have them except for `effects`).
  </action>
  <verify>Read the file and confirm `duration: number;` appears after `activityType` line.</verify>
  <done>Activity interface has required duration field of type number.</done>
</task>

<task type="auto">
  <name>Task 2: Add duration to all activities in activity.service.ts</name>
  <files>src/app/game-state/activity.service.ts</files>
  <action>
Add `duration: 1,` to all 46 activity definitions in activity.service.ts.

For each activity object literal (this.OddJobs, this.Resting, etc.), add the duration field after `activityType: ActivityType.XXX,` and before the `description:` line.

Pattern to follow:
```typescript
activityType: ActivityType.XXX,
duration: 1,
description: [...]
```

Work through systematically. Use TypeScript compiler errors to find any missed activities - run `npx ng build` to verify.
  </action>
  <verify>Run `npx ng build` - should have fewer errors than before Task 1 (or none if all fixed). Grep for `activityType:` and verify each has a following `duration: 1,` line.</verify>
  <done>All 46 activities in activity.service.ts have duration: 1 field.</done>
</task>

<task type="auto">
  <name>Task 3: Add duration to all activities in hell.service.ts</name>
  <files>src/app/game-state/hell.service.ts</files>
  <action>
Add `duration: 1,` to all 23 activity definitions in hell.service.ts.

This includes:
- 20 static activity properties (burnMoney, hellRecruiting, rehabilitation, etc.)
- 3 dynamic activities:
  - `flee()` method return object
  - `setEnterHellsArray()` - Hell portal activity object literal (inside forEach)
  - `setEnterHellsArray()` - FinishHell activity object literal

For each activity, add `duration: 1,` after `activityType:` and before `description:`.

Use TypeScript compiler to verify all activities are updated - `npx ng build` should compile cleanly.
  </action>
  <verify>Run `npx ng build` - should compile with no errors related to missing duration field. Grep for `activityType:` in hell.service.ts and verify each has duration: 1.</verify>
  <done>All 23 activities in hell.service.ts have duration: 1 field.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `npx ng build` completes with no errors
2. Interface check: `duration: number` exists in Activity interface
3. Coverage check: All activity definitions have `duration: 1`
4. Runtime check: Game starts and basic activity works (optional sanity check)
</verification>

<success_criteria>
- Activity interface has `duration: number` field
- All 69 activity definitions include `duration: 1`
- `npx ng build` succeeds with no TypeScript errors
- No behavioral changes (game runs exactly as before)
</success_criteria>

<output>
After completion, create `.planning/phases/01-duration-foundation/01-01-SUMMARY.md`
</output>
