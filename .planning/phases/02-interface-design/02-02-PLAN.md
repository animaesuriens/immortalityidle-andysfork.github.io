---
phase: 02-interface-design
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/effects/formulas/formula.builders.ts
  - src/app/effects/handlers/handler.interface.ts
  - src/app/effects/utils/exhaustive.ts
  - src/app/effects/index.ts
autonomous: true

must_haves:
  truths:
    - "Formula builder function signatures exist (attr, add, mult, log2, etc.)"
    - "EffectHandler interface defines execute() and render() signatures"
    - "HandlerRegistry type maps effect types to typed handlers"
    - "assertNever utility function enables exhaustive switch checking"
    - "TypeScript compiles with stubs only (no implementation)"
  artifacts:
    - path: "src/app/effects/formulas/formula.builders.ts"
      provides: "Formula builder function signatures"
      contains: "export function attr"
      min_lines: 50
    - path: "src/app/effects/handlers/handler.interface.ts"
      provides: "EffectHandler interface and HandlerRegistry type"
      contains: "export interface EffectHandler"
    - path: "src/app/effects/utils/exhaustive.ts"
      provides: "assertNever helper"
      contains: "export function assertNever"
    - path: "src/app/effects/index.ts"
      provides: "Barrel export for public API"
      contains: "export * from"
  key_links:
    - from: "src/app/effects/formulas/formula.builders.ts"
      to: "src/app/effects/types/formula.types.ts"
      via: "import Formula"
      pattern: "import.*Formula.*from.*formula\\.types"
    - from: "src/app/effects/handlers/handler.interface.ts"
      to: "src/app/effects/types/effect.types.ts"
      via: "import Effect, EffectType"
      pattern: "import.*Effect.*from.*effect\\.types"
    - from: "src/app/effects/index.ts"
      to: "multiple type files"
      via: "barrel exports"
      pattern: "export \\* from"
---

<objective>
Create the formula builders, handler interface, and exhaustive check utility to complete the interface design.

Purpose: Provide the function signatures and interfaces that will be implemented in Phase 3. Establishes the handler registry pattern and formula builder DSL.

Output: Four files completing the effects module plus verified TypeScript compilation.
</objective>

<execution_context>
@C:\Users\marce\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\marce\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DECISIONS.md
@.planning/phases/02-interface-design/02-RESEARCH.md
@.planning/phases/02-interface-design/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create formula builder stubs</name>
  <files>
    src/app/effects/formulas/formula.builders.ts
  </files>
  <action>
Create formula builder function signatures using the exact code from 02-RESEARCH.md "Formula Builder Signatures" section.

Include all builder functions with stub implementations that throw:

**Value references:**
- `attr(attribute: AttributeType): Formula` - reference an attribute value
- `status(status: StatusType): Formula` - reference a status value
- `statusMax(status: StatusType): Formula` - reference a status max value
- `fixed(value: number): Formula` - a fixed numeric constant
- `variable(name: string): Formula` - reference a runtime variable

**Arithmetic operations:**
- `add(...operands: (Formula | number)[]): Formula`
- `sub(left: Formula | number, right: Formula | number): Formula`
- `mult(...operands: (Formula | number)[]): Formula`
- `div(left: Formula | number, right: Formula | number): Formula`

**Mathematical functions:**
- `log2(operand: Formula | number): Formula`
- `ln(operand: Formula | number): Formula`
- `sqrt(operand: Formula | number): Formula`
- `floor(operand: Formula | number): Formula`
- `pow(base: Formula | number, exponent: Formula | number): Formula`
- `exp(operand: Formula | number): Formula`

**Comparison/conditional:**
- `min(...operands: (Formula | number)[]): Formula`
- `max(...operands: (Formula | number)[]): Formula`

Every function body must be: `throw new Error('Not implemented - Phase 2 interface only');`

Import Formula from '../types/formula.types'.
Import AttributeType, StatusType from '../../game-state/character'.

Include JSDoc comments explaining what each function does, what it evaluates to, and how it renders.
  </action>
  <verify>
- Run `npx tsc --noEmit src/app/effects/formulas/formula.builders.ts` (no errors)
- Verify file has 17 exported functions
  </verify>
  <done>
formula.builders.ts exists with all 17 builder function signatures, each throwing 'Not implemented'.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create handler interface and utility</name>
  <files>
    src/app/effects/handlers/handler.interface.ts
    src/app/effects/utils/exhaustive.ts
  </files>
  <action>
**File 1: handler.interface.ts**

Create using the exact code from 02-RESEARCH.md "Handler Interface" section.

Define:
- `RenderFormat` type: `'short' | 'long' | 'formula'`
- `EffectHandler<T extends Effect>` interface with:
  - `execute(effect: T, context: EffectContext): void`
  - `render(effect: T, context: EffectContext, format: RenderFormat): string`
- `HandlerRegistry` type mapping each effect type to its typed handler:
  `{ [K in EffectType]: EffectHandler<Extract<Effect, { type: K }>> }`

Import Effect, EffectType from '../types/effect.types'.
Import EffectContext from '../types/context.types'.

Include JSDoc comments explaining the purpose of each type and method.

**File 2: exhaustive.ts**

Create using the exact code from 02-RESEARCH.md "Exhaustive Check Utility" section.

Define:
- `assertNever(x: never, message?: string): never` - throws error if called at runtime, catches missing cases at compile time

Include JSDoc with @example showing usage in a switch statement.
  </action>
  <verify>
- Run `npx tsc --noEmit src/app/effects/handlers/handler.interface.ts` (no errors)
- Run `npx tsc --noEmit src/app/effects/utils/exhaustive.ts` (no errors)
  </verify>
  <done>
handler.interface.ts exports EffectHandler, RenderFormat, and HandlerRegistry. exhaustive.ts exports assertNever.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create barrel export and verify full build</name>
  <files>
    src/app/effects/index.ts
  </files>
  <action>
Create the barrel export file that re-exports the public API:

```typescript
// src/app/effects/index.ts

// Type definitions
export * from './types/effect.types';
export * from './types/condition.types';
export * from './types/formula.types';
export * from './types/context.types';

// Formula builders
export * from './formulas/formula.builders';

// Handler interface
export * from './handlers/handler.interface';

// Utilities
export * from './utils/exhaustive';
```

After creating, run a full Angular build to verify the entire module compiles:

```bash
ng build --configuration=development
```

This validates:
- All imports between files resolve correctly
- No circular dependencies
- Types are properly exported
- Angular can find all files

If build fails, analyze error messages and fix issues in the type files.
  </action>
  <verify>
- `ng build --configuration=development` completes with no errors
- Can import from '@app/effects' (or '../effects' from sibling modules)
  </verify>
  <done>
index.ts barrel export exists. Full Angular build succeeds. Effects module is ready for implementation in Phase 3.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `ng build --configuration=development` - must succeed with no errors
2. Verify all 8 files exist in src/app/effects/ hierarchy
3. Verify no runtime behavior changed (no existing code imports effects yet)
4. Run existing tests: `ng test --watch=false --browsers=ChromeHeadless` - should pass unchanged
</verification>

<success_criteria>
- All 8 files exist in src/app/effects/ hierarchy
- 17 formula builder functions exported with stub implementations
- EffectHandler interface exported with execute() and render() signatures
- HandlerRegistry type correctly maps effect types to typed handlers
- assertNever utility exported
- index.ts barrel export re-exports all public types
- `ng build` succeeds with no errors
- Existing tests still pass (no behavior change)
</success_criteria>

<output>
After completion, create `.planning/phases/02-interface-design/02-02-SUMMARY.md`
</output>
