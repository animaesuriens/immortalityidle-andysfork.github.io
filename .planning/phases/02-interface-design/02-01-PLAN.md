---
phase: 02-interface-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/app/effects/types/effect.types.ts
  - src/app/effects/types/condition.types.ts
  - src/app/effects/types/formula.types.ts
  - src/app/effects/types/context.types.ts
autonomous: true

must_haves:
  truths:
    - "Effect union type exists with all 14 effect kinds as discriminated variants"
    - "Condition union type exists with all 8 condition kinds"
    - "Formula interface defines evaluate() and render() methods"
    - "EffectContext interface specifies all service access points"
  artifacts:
    - path: "src/app/effects/types/effect.types.ts"
      provides: "Effect discriminated union with 14 variants"
      contains: "export type Effect ="
    - path: "src/app/effects/types/condition.types.ts"
      provides: "Condition discriminated union"
      contains: "export type Condition ="
    - path: "src/app/effects/types/formula.types.ts"
      provides: "Formula interface and FormulaContext"
      contains: "export interface Formula"
    - path: "src/app/effects/types/context.types.ts"
      provides: "EffectContext interface"
      contains: "export interface EffectContext"
  key_links:
    - from: "src/app/effects/types/effect.types.ts"
      to: "src/app/effects/types/formula.types.ts"
      via: "import Formula"
      pattern: "import.*Formula.*from.*formula\\.types"
    - from: "src/app/effects/types/effect.types.ts"
      to: "src/app/effects/types/condition.types.ts"
      via: "import Condition"
      pattern: "import.*Condition.*from.*condition\\.types"
---

<objective>
Create the foundational type definitions for the declarative effects system.

Purpose: Define all discriminated union types that will enable compile-time exhaustive checking of effect handling. This is the "single source of truth" foundation.

Output: Four type definition files in src/app/effects/types/ plus expr-eval package installed.
</objective>

<execution_context>
@C:\Users\marce\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\marce\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DECISIONS.md
@.planning/phases/02-interface-design/02-RESEARCH.md
@src/app/game-state/character.ts (for AttributeType, StatusType, EquipmentPosition imports)
@src/app/game-state/inventory.service.ts (for Item, Equipment, Pill types)
@src/app/game-state/log.service.ts (for LogTopic type)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install expr-eval and create directory structure</name>
  <files>
    package.json
    package-lock.json
    src/app/effects/types/ (directory)
  </files>
  <action>
Install the expr-eval library for mathematical expression evaluation:

```bash
npm install expr-eval
```

Create the effects directory structure:

```bash
mkdir -p src/app/effects/types
mkdir -p src/app/effects/formulas
mkdir -p src/app/effects/handlers
mkdir -p src/app/effects/utils
```

Verify expr-eval was added to package.json dependencies.
  </action>
  <verify>
- Run `npm ls expr-eval` to confirm installed
- Run `ls src/app/effects/` to confirm directories exist
  </verify>
  <done>
expr-eval is in package.json dependencies and effects directory structure exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create core type definition files</name>
  <files>
    src/app/effects/types/formula.types.ts
    src/app/effects/types/condition.types.ts
    src/app/effects/types/effect.types.ts
    src/app/effects/types/context.types.ts
  </files>
  <action>
Create four type definition files using the exact code from 02-RESEARCH.md "Code Examples" section.

**File 1: formula.types.ts**
Create the Formula interface with evaluate() and render() methods, FormulaContext interface, and FormulaRenderFormat type. Import AttributeType and StatusType from character.ts.

**File 2: condition.types.ts**
Create the Condition discriminated union with 8 variants: FlagCondition, AttributeCondition, StatusCondition, FurnitureCondition, InventoryCondition, AndCondition, OrCondition, NotCondition. Import AttributeType and StatusType from character.ts, Formula from formula.types.ts.

**File 3: effect.types.ts**
Create the Effect discriminated union with 14 variants:
- AttributeEffect, StatusEffect, MoneyEffect
- ItemAddEffect, ItemConsumeEffect, ItemGenerateEffect
- ConditionalEffect, ChanceEffect, ProgressEffect
- SpawnEnemyEffect, SpawnFollowerEffect
- YinYangEffect, TriggerBattleEffect, LifespanEffect

Import from formula.types.ts and condition.types.ts. Also define EnemyConfig interface here (not in context.types.ts to avoid circular deps).

**File 4: context.types.ts**
Create the EffectContext interface with:
- Read-only state access (attributes, status, money, unlock flags, yin/yang, variables)
- Mutation methods (increaseAttribute, updateMoney, modifyStatus, etc.)
- Item operations (addItem, consumeItem, generateWeapon, etc.)
- Progress operations (incrementProgress, checkProgressCompletion)
- Spawn operations (spawnEnemy, spawnFollower, triggerBattle)
- Home/furniture checks (hasFurniture, getFurnitureId)
- Logging (log, logInjury)

Also define AttributeValue, StatusValue, and FurnitureSlot types here.

Import AttributeType, StatusType, EquipmentPosition from character.ts.
Import Equipment and Pill from inventory.service.ts (check exact export names).
Import LogTopic from log.service.ts.

IMPORTANT: All methods in EffectContext are interface signatures only - no implementation.
  </action>
  <verify>
- Run `npx tsc --noEmit src/app/effects/types/formula.types.ts` (no errors)
- Run `npx tsc --noEmit src/app/effects/types/condition.types.ts` (no errors)
- Run `npx tsc --noEmit src/app/effects/types/effect.types.ts` (no errors)
- Run `npx tsc --noEmit src/app/effects/types/context.types.ts` (no errors)
  </verify>
  <done>
All four type files exist and compile with TypeScript. Effect union has 14 variants, Condition union has 8 variants, Formula has evaluate/render, EffectContext has all service access signatures.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `ng build --configuration=development` - should compile with no errors
2. Verify each type file exports correctly named types
3. Verify imports between files work (effect.types imports from formula.types and condition.types)
</verification>

<success_criteria>
- expr-eval package installed in package.json
- src/app/effects/types/ directory exists with 4 files
- All files use discriminated unions with `readonly type` discriminant
- TypeScript compiles with no errors
- No runtime code - only type definitions and interfaces
</success_criteria>

<output>
After completion, create `.planning/phases/02-interface-design/02-01-SUMMARY.md`
</output>
